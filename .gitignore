# Rendered mindmap artifacts (generated by the skill, not source)
*.mindmap.html
# ...but keep the curated example render
!examples/*.mindmap.html

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only used transiently by `npx markmap-cli` during --render)
node_modules/

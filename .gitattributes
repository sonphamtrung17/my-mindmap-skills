# Normalize text to LF in the repo; keep shell scripts strictly LF on checkout
# (CRLF in a .sh file breaks bash with `\r` errors).
* text=auto eol=lf

*.sh    text eol=lf
*.md    text eol=lf
*.json  text eol=lf
